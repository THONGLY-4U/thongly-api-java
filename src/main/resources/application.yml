spring:
  application:
    name: product-web-api
  datasource:
    url: jdbc:postgresql://dpg-d3tfifbe5dus738tnekg-a.oregon-postgres.render.com:5432/dbsu6_c8rw
   
    username: thongly
    password: iPye1zmW92Pc7Aik5lP9A8D2l97P24l9
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: ${DB_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
        format_sql: true
    open-in-view: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 500MB

server:
  port: ${PORT:8080}
  error:
    include-message: always

file:
  upload-dir: ${FILE_UPLOAD_DIR:./uploads}

logging:
  level:
    com.setec: INFO
    org.hibernate.SQL: WARN